You must have NodeJS and NPM installed - obvs!

To install simply run "sudo npm install" and the "package.json" will do the 
rest. 

You must also set the user "PI" to have access to the GPIO pins.

Happy Hacking!
